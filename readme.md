** Cakephp FileSystem Plugin **

The FileSystem plugin is a building-block plugin that should be the de-facto standard for file system processing and storage. 

TODO: add tests, better function documentation